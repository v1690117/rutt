import styled from 'styled-components';
import { TestSuiteCardItemPropsType } from '../../../../../types/types';

export const TestSuiteCardItemIdWrapper = styled.div<TestSuiteCardItemPropsType>`
text-align: start;
width: 64px;
cursor: pointer;    
`;
