import styled from 'styled-components';
import { TestSuiteCardItemPropsType } from '../../../../../../types/types';

export const TestSuiteCardItemTitleWrapper = styled.div<TestSuiteCardItemPropsType>`
text-align: start;
width: 240px;
font-weight: 700;
`;
